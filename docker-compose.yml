version: '3.8'

services:
  myzone:
    build: .
    container_name: myzone-dashboard
    restart: unless-stopped
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./dashboard/widgets.json:/app/dashboard/widgets.json
      - .tmp_data:/app/.tmp

volumes:
  .tmp_data:
